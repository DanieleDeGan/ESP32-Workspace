# Arduino IDE / build
build/
*.bin
*.elf
*.map

# Python: gli strumenti in tools/ si importano fra loro, e l'import lascia i
# .pyc. Ci sono finiti dentro una volta con un git add -A.
__pycache__/
*.pyc

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Editor
.vscode/

# Claude Code (impostazioni locali, non condivise)
.claude/settings.local.json

# Credenziali reali: MAI committarle (questo repo e' pubblico).
# Versionati ci sono solo i *.example, con i segnaposto: copiali in secrets.h e
# riempili con la tua rete. La riga qui sotto NON e' un commento.
#
# E' un pattern, non un percorso: vale per QUALUNQUE cartella, presente o
# futura, dentro e fuori dal repo (prima erano tre percorsi letterali, che
# smettevano di coprire lo sketch appena lo si spostava o copiava).
secrets.h
