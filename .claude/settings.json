{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(python -c \"import sys,json;p=(json.load(sys.stdin).get('tool_input') or {}).get('file_path') or '';print(p.replace(chr(92),'/'))\"); case \"$f\" in */MeteoHub_S3/www/dither.html|*/MeteoHub_S3/www/analisi.html) n=$(basename \"$f\" .html); cd \"$(dirname \"$f\")/..\" && python www/gen_page.py \"$n\";; esac",
            "timeout": 30,
            "statusMessage": "Rigenero la pagina in PROGMEM"
          }
        ]
      }
    ]
  }
}
